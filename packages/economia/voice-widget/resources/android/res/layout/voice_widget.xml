<?xml version="1.0" encoding="utf-8"?>
<!-- Layout del widget (RemoteViews: solo vistas soportadas). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/voice_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="8dp"
    android:clickable="true"
    android:focusable="true"
    android:background="@drawable/voice_widget_bg">

    <ImageView
        android:id="@+id/voice_widget_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_voice_mic"
        android:contentDescription="Registrar por voz" />

    <TextView
        android:id="@+id/voice_widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Hablar"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold" />
</LinearLayout>
